# Differentiable Pendulum (Python)

Infer double pendulum parameters from noisy observations using finite-difference gradients.

## Running

```bash
cd py/differentiable_pendulum
pip install -r requirements.txt
python main.py
```
