# Spin Glass Neural Network — Python

Tracks Hessian eigenvalue spectrum and IPR during training to probe the glass-to-order transition.

## Usage

```bash
cd py/spin_glass_nn
pip install -r requirements.txt
python main.py
```
