# Percolation on Simplicial Complexes — Python

Percolation phase transitions on clique complexes with higher-order interactions.

## Usage

```bash
cd py/percolation_simplicial
pip install -r requirements.txt
python main.py
```
